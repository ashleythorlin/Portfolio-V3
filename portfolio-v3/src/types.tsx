
export type ActiveNav = {
    id: string,
    index: number
}

export type DisplayInfo = {
    display: string
}

export type SkillData = {
    name: string,
    desc: string[]
}

export type ProjectData = {
  name: string,
  desc: string[]
}